const seed={memories:[],videos:[],music:[],texts:[]};
function load(){try{return JSON.parse(localStorage.getItem('jojo_content'))||seed}catch{return seed}}
function card(x,type){const title=x.title||'بدون عنوان';const desc=x.description||x.text||'';if(type==='video'&&x.url)return `<article class="card"><h3>${esc(title)}</h3><video controls preload="metadata" src="${attr(x.url)}"></video><p>${esc(desc)}</p></article>`;if(type==='music'&&x.url)return `<article class="card"><h3>${esc(title)}</h3><audio controls preload="metadata" src="${attr(x.url)}"></audio><p>${esc(desc)}</p></article>`;if(type==='memory'&&x.url)return `<article class="card"><img loading="lazy" src="${attr(x.url)}" alt="${attr(title)}"><h3>${esc(title)}</h3><p>${esc(desc)}</p></article>`;return `<article class="card"><h3>${esc(title)}</h3><p>${esc(desc)}</p></article>`}
function esc(v){return String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function attr(v){return esc(v)}
function render(){const d=load();const sets=[['memoryGrid',d.memories,'memory'],['videoGrid',d.videos,'video'],['musicGrid',d.music,'music']];sets.forEach(([id,a,t])=>{const el=document.getElementById(id);el.innerHTML=a.length?a.map(x=>card(x,t)).join(''):`<div class="empty">هنوز چیزی در این بخش قرار نگرفته است.</div>`});const tx=document.getElementById('textGrid');tx.innerHTML=d.texts.length?d.texts.map(x=>`<article class="letter"><h3>${esc(x.title||'نوشته')}</h3><p>${esc(x.text||x.description||'')}</p></article>`).join(''):`<div class="empty">هنوز نوشته‌ای اضافه نشده است.</div>`}
render();
